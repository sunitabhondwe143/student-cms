<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SMS | <%= page.replace(/_/g, ' ') %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  <style>
    :root {
      --sidebar-bg: #1B1C31;
      --sidebar-hover: #2B2DFB;
      --bg-color: #F4F7FE;
      --text-main: #2B3674;
      --text-muted: #A3AED0;
      --card-radius: 20px;
    }
    body { font-family: 'Inter', sans-serif; background: var(--bg-color); color: var(--text-main); }

    /* Sidebar */
    .sidebar {
      position: fixed; top: 0; left: 0; bottom: 0;
      width: 280px; background: var(--sidebar-bg);
      padding: 25px 18px; overflow-y: auto; z-index: 100;
      display: flex; flex-direction: column;
    }
    .brand-box { display: flex; align-items: center; gap: 12px; margin-bottom: 30px; padding: 0 8px; }
    .brand-logo {
      width: 42px; height: 42px; border-radius: 12px;
      background: linear-gradient(135deg, #FF9C00, #FFC048);
      display: flex; align-items: center; justify-content: center;
      color: #fff; font-weight: 700; font-size: 20px;
    }
    .brand-text h6 { color: #fff; margin: 0; font-weight: 700; }
    .brand-text small { color: var(--text-muted); font-size: 11px; }
    .nav-header {
      color: var(--text-muted); font-size: 11px; font-weight: 600;
      text-transform: uppercase; letter-spacing: 1px; margin: 18px 8px 8px 8px;
    }
    .side-link {
      display: flex; align-items: center; gap: 12px;
      color: #B8B9CF; text-decoration: none; padding: 11px 14px;
      border-radius: 12px; font-size: 14px; margin-bottom: 4px; transition: all 0.2s;
    }
    .side-link i { width: 18px; text-align: center; }
    .side-link:hover { background: rgba(43, 45, 251, 0.25); color: #fff; }
    .side-link.active { background: var(--sidebar-hover); color: #fff; }
    .side-link.logout { color: #FF6B6B; }
    .side-link.logout:hover { background: rgba(255, 107, 107, 0.15); color: #FF6B6B; }
    .trophy-card {
      margin-top: auto; background: linear-gradient(135deg, #2B2DFB, #6366F1);
      border-radius: 16px; padding: 20px; text-align: center; color: #fff; margin-bottom: 12px;
    }

    /* Main */
    .main { margin-left: 280px; padding: 25px 30px; }
    .topbar { display: flex; align-items: center; justify-content: space-between; margin-bottom: 25px; flex-wrap: wrap; gap: 15px; }
    .page-title { font-weight: 700; font-size: 24px; text-transform: capitalize; margin: 0; }
    .topbar-right { display: flex; align-items: center; gap: 10px; background: #fff; border-radius: 50px; padding: 8px 12px; box-shadow: 0 5px 20px rgba(112,144,176,0.1); }
    .search-input { border: none; background: #F4F7FE; border-radius: 50px; padding: 8px 16
